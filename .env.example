# Environment Variables Setup

Copy this file to `.env.local` and fill in your actual values.

```bash
# n8n Webhook Configuration
REACT_APP_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id
REACT_APP_N8N_API_KEY=your-api-key-if-needed

# Supabase Configuration (Optional - for future backend integration)
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key
```

## Setting up n8n Webhook with Gemini API

1. **Create n8n Account**: https://n8n.io
2. **Create Workflow**:
   - Add Webhook node (Trigger)
   - Add HTTP Request node to call Gemini API
   - Map request body to Gemini payload
   - Add Response node to return reply

3. **Get Webhook URL**: Copy from Webhook node settings

4. **Gemini API Setup**:
   - Get Gemini API key from Google AI Studio: https://aistudio.google.com/app/apikeys
   - Add to n8n as environment variable or in HTTP Request node

5. **Test**: Use curl or Postman to test webhook:
```bash
curl -X POST https://your-n8n-webhook.url \
  -H "Content-Type: application/json" \
  -d '{"message":"Hello"}'
```

6. **Add to .env.local**: Copy webhook URL to `REACT_APP_N8N_WEBHOOK_URL`
